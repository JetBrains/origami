apiVersion: v1
kind: Service {# https://kubernetes.io/docs/user-guide/services/ #}
metadata:
  name: {{ app_name }}
spec:
  type: ClusterIP
  ports:
    - name: http 
      port: 80
      targetPort: {{ app_port }}
  selector:
    app: {{ app_name }}
